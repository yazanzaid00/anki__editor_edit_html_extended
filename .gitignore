# Ignore everything by default
*

# --- Whitelist: Un-ignore specific files and directories ---

# .gitignore itself
!.gitignore

# Keep the main source directory.
# All files and subdirectories within src/ are necessary for the add-on's runtime.
!src/

# Files to keep at the root
!LICENSE_AGPL
!LICENSE_APACHEv2
!LICENSE_GPL
!README.md
!manifest.json # For standalone .ankiaddon distribution if manifest is at root
!ankiweb.html # Optional: for GitHub-hosted AnkiWeb listing

# --- Standard Ignores (apply everywhere) ---

# Python byte code and cache
__pycache__/
*.py[cod]
*$py.class

# macOS system files
.DS_Store

# Development artifacts & IDE temp files
designer/
screenshots/
tempCodeRunnerFile.*
*.swp
*.swo
